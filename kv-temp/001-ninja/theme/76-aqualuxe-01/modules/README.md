# Modules

Each subfolder is a self-contained feature module. Include a `bootstrap.php` that hooks into WordPress when the module is enabled.

Enable/disable modules by adding/removing folders or extending the loader to read a config option.
