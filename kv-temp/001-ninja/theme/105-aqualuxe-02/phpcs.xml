<?xml version="1.0"?>
<ruleset name="AquaLuxe WordPress Coding Standards">
    <description>Coding standards for AquaLuxe WordPress theme</description>

    <!-- What to scan -->
    <file>.</file>

    <!-- How to scan -->
    <arg value="sp"/> <!-- Show sniff and progress -->
    <arg name="basepath" value="./"/> <!-- Strip the file paths down to the relevant bit -->
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg name="parallel" value="8"/> <!-- Enables parallel processing when available for faster results -->

    <!-- Rules: Check PHP version compatibility -->
    <config name="minimum_supported_wp_version" value="6.0"/>
    <config name="testVersion" value="7.4-"/>

    <!-- Rules: WordPress Coding Standards -->
    <rule ref="WordPress">
        <!-- Allow . in hook names -->
        <exclude name="WordPress.NamingConventions.ValidHookName.UseUnderscores"/>
        <!-- Allow short array syntax -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
        <!-- Allow multiple assignments -->
        <exclude name="Squiz.PHP.DisallowMultipleAssignments"/>
    </rule>

    <!-- Rules: WordPress Extra -->
    <rule ref="WordPress-Extra">
        <!-- Allow . in hook names -->
        <exclude name="WordPress.NamingConventions.ValidHookName.UseUnderscores"/>
    </rule>

    <!-- Rules: WordPress Documentation -->
    <rule ref="WordPress-Docs"/>

    <!-- Verify that the text_domain is set to the desired text-domain -->
    <rule ref="WordPress.WP.I18n">
        <properties>
            <property name="text_domain" type="array" value="aqualuxe"/>
        </properties>
    </rule>

    <!-- Allow for theme specific exceptions to WordPress filename conventions -->
    <rule ref="WordPress.Files.FileName">
        <properties>
            <property name="strict_class_file_names" value="false"/>
            <property name="is_theme" value="true"/>
        </properties>
    </rule>

    <!-- Set the minimum supported WP version -->
    <rule ref="WordPress.WP.DeprecatedFunctions">
        <properties>
            <property name="minimum_supported_version" value="6.0"/>
        </properties>
    </rule>

    <!-- Exclude the build, vendor, and node_modules directories -->
    <exclude-pattern>/assets/dist/*</exclude-pattern>
    <exclude-pattern>/node_modules/*</exclude-pattern>
    <exclude-pattern>/vendor/*</exclude-pattern>
    <exclude-pattern>*.min.js</exclude-pattern>
    <exclude-pattern>*.min.css</exclude-pattern>

    <!-- Include the WordPress-Extra standard -->
    <rule ref="WordPress-Extra">
        <!-- Allow . in hook names -->
        <exclude name="WordPress.NamingConventions.ValidHookName.UseUnderscores"/>
        <!-- Allow array short syntax -->
        <exclude name="Generic.Arrays.DisallowShortArraySyntax"/>
    </rule>

    <!-- Check for PHP cross-version compatibility -->
    <rule ref="PHPCompatibilityWP"/>
</ruleset>