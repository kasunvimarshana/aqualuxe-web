# Installation

1. Copy `aqualuxe` theme folder into `wp-content/themes/`.
2. Activate in WP Admin > Appearance > Themes.
3. In the theme folder run:
   - `npm install`
   - `npm run build`
4. Optional: Tools > AquaLuxe Importer to import demo content.
