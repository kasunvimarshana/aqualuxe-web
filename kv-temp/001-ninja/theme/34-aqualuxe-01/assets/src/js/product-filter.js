/**
 * Product Filter
 * 
 * Main entry point for the product filter functionality.
 */

import ProductFilter from './modules/product-filter';

// Initialize the product filter when the DOM is ready
document.addEventListener('DOMContentLoaded', function() {
    ProductFilter.init();
});