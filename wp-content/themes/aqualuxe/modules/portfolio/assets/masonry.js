// Portfolio Masonry Grid (optional, for future enhancement)
document.addEventListener('DOMContentLoaded', function() {
  const grid = document.querySelector('.portfolio-grid');
  if (!grid) return;
  // Simple CSS grid fallback; for advanced masonry, integrate a library or custom logic here.
});
